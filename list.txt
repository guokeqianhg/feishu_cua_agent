CUA-Lark 后端任务清单、测试方式与下一步计划
版本：v5
更新时间：2026-04-30 19:09

说明：
本文档基于当前 backend 代码、README.md、ACCEPTANCE.md、任务规格文档和最近真实运行报告整理。
所有命令都应先执行：

```powershell
conda activate agent
cd D:\找工作\feishu_cua_agent\backend
```


一、当前项目目标

CUA-Lark 后端目标是实现一个飞书桌面 GUI 智能测试 Agent。

它不调用飞书开放平台 API 完成任务，而是像真实用户一样：
1. 截图观察当前桌面。
2. 理解自然语言任务。
3. 将任务路由到 IM、Docs、Calendar 等子产品 workflow。
4. 用鼠标、键盘真实操作飞书桌面端和浏览器文档编辑器。
5. 每一步截图验证。
6. 生成可回放的 summary.md、summary.json、steps.jsonl 和截图证据。


二、对照任务规格的当前完成情况

任务规格中要求重点覆盖：
1. 日历 Calendar：创建日程、邀请参会人、修改日程时间、查看忙闲状态。
2. 云文档 Docs：创建文档、编辑文本内容、插入标题/列表、分享文档。
3. IM：发送文本/图片消息、创建群组、@ 提及、搜索消息记录、表情回复。

当前状态：
1. IM 六项核心能力最新真实回归全部通过：文本、图片、建群、@ 提及、消息搜索、表情回复。
2. Docs 创建文档、富文本编辑和分享文档最新真实回归全部通过。
3. Calendar 四项能力均有较早真实通过报告；最近一轮回归四项均失败，需要下一轮优先修复。
4. 仍需要继续做多轮回归，尤其是 Docs 浏览器编辑器前台状态、分享弹窗、Calendar 参会人搜索、忙闲订阅状态、日历编辑器残留状态这类受账号状态影响的界面。


三、最新真实回归矩阵

统计时间：2026-04-30 19:09。
口径：优先列最近一次真实报告；如果最近一次失败，同时列最后已知通过报告。

IM：
1. 发送文本消息：最新通过，`runs\reports\run_20260426_173819_7c6c0263`，7/7 步通过。
2. 发送图片消息：最新通过，`runs\reports\run_20260426_174305_4aaa8145`，7/7 步通过。
3. 创建群组：最新通过，`runs\reports\run_20260426_183354_ac0c06f0`，10/10 步通过。
4. @ 提及：最新通过，`runs\reports\run_20260426_180101_f8712542`，9/9 步通过。
5. 搜索消息记录：最新通过，`runs\reports\run_20260426_174845_c9d4c0b4`，6/6 步通过。
6. 表情回复：最新通过，`runs\reports\run_20260426_184529_54dfb4ac`，9/10 步通过，1 步条件跳过。

Docs：
1. 创建文档：最新通过，`runs\reports\run_20260430_185053_44f5eb5f`，12/15 步通过，3 步条件跳过。
2. 编辑文本内容 / 插入标题和列表：最新通过，`runs\reports\run_20260430_190149_573da0e8`，14/17 步通过，3 步条件跳过。
3. 分享文档：最新通过，`runs\reports\run_20260430_190926_55597bc7`，21/24 步通过，3 步条件跳过。

Calendar：
1. 创建日程：最近失败，`runs\reports\run_20260426_165223_0bd0a1f3`，失败原因为 Calendar attendee search result was not locally confirmed by OCR；最后已知通过 `runs\reports\run_20260426_110901_06bdf40b`。
2. 邀请参会人：最近失败，`runs\reports\run_20260426_165410_4c3ec35b`，失败原因为 Calendar attendee search result was not locally confirmed by OCR；最后已知通过 `runs\reports\run_20260426_130145_6805e8ae`。
3. 修改日程时间：最近失败，`runs\reports\run_20260426_165551_430bb4be`，失败原因为最终 OCR 未找到日程标题且修改后时间未命中；最后已知通过 `runs\reports\run_20260426_120155_ad2c1bea`。
4. 查看忙闲状态：最近失败，`runs\reports\run_20260426_165749_76613279`，失败原因为 2026-04-27 10:00 附近未看到李新元忙闲条目；最后已知通过 `runs\reports\run_20260426_152523_2aec6992`。


四、Docs 当前进展

1. 创建文档：已真实验证通过

case：
`cases\docs_create_doc.yaml`

最新真实通过报告：
`runs\reports\run_20260426_164952_cfa7cc19`

能力：
1. 打开飞书云文档。
2. hover “新建”打开菜单。
3. OCR 定位并点击“文档”。
4. 点击“新建空白文档”加号卡片。
5. 等待浏览器文档编辑器打开。
6. 输入标题和正文。
7. 验证标题和正文可见。

推荐命令：

```powershell
$env:DRY_RUN="false"
$env:CUA_LARK_PLACEHOLDER_SCREENSHOT="false"
$env:CUA_LARK_MODEL_PROVIDER="auto"
$env:CUA_LARK_ALLOW_DOC_CREATE="true"
python -B cli.py run-case cases\docs_create_doc.yaml --auto-debug
```


2. 编辑文本内容 / 插入标题和列表：能力已实现，最近回归失败

case：
`cases\docs_rich_edit_guarded.yaml`

最后已知真实通过报告：
`runs\reports\run_20260426_092617_6d2d3742`

最近失败报告：
`runs\reports\run_20260426_165144_960cbbc1`

当前判断：
1. 能力曾真实通过，标题、正文、标题块和列表项均可见。
2. 最近一轮卡在 Docs screen 本地确认，属于入口/前台状态回归问题，不是富文本编辑步骤本身失败。

能力：
1. 新建测试文档。
2. 输入正文。
3. 插入标题“本周进展”。
4. 插入列表项“完成IM扩展、调试Calendar、调试Docs”。
5. OCR/视觉验证富文本内容存在。

推荐命令：

```powershell
$env:DRY_RUN="false"
$env:CUA_LARK_PLACEHOLDER_SCREENSHOT="false"
$env:CUA_LARK_MODEL_PROVIDER="auto"
$env:CUA_LARK_ALLOW_DOC_CREATE="true"
python -B cli.py run-case cases\docs_rich_edit_guarded.yaml --auto-debug
```


3. 分享文档：能力已实现，最近回归失败

case：
`cases\docs_share_doc_guarded.yaml`

最后已知真实通过报告：
`runs\reports\run_20260426_105229_bbd6d4ed`

最近失败报告：
`runs\reports\run_20260426_165204_744838e6`

当前判断：
1. 分享能力曾真实通过，最终验证为 Docs share flow completed and final share dialog is stable。
2. 最近一轮卡在 Docs screen 本地确认，下一轮应优先修复 Docs 入口/浏览器编辑器前台状态。

能力：
1. 新建测试文档。
2. 输入标题和正文。
3. 打开分享弹窗。
4. 搜索分享对象“李新元”。
5. 选择搜索结果并添加。
6. 点击最终确认/分享按钮。
7. 验证收件人搜索状态关闭，分享流程稳定结束。

推荐命令：

```powershell
$env:DRY_RUN="false"
$env:CUA_LARK_PLACEHOLDER_SCREENSHOT="false"
$env:CUA_LARK_MODEL_PROVIDER="auto"
$env:CUA_LARK_ALLOW_DOC_CREATE="true"
$env:CUA_LARK_ALLOW_DOC_SHARE="true"
$env:CUA_LARK_ALLOWED_DOC_SHARE_RECIPIENT="李新元"
python -B cli.py run-case cases\docs_share_doc_guarded.yaml --auto-debug
```


五、Calendar 当前进展

1. 创建日程：能力已实现，最近回归失败

case：
`cases\calendar_create_event.yaml`

最后已知真实通过报告：
`runs\reports\run_20260426_110901_06bdf40b`

最近失败报告：
`runs\reports\run_20260426_165223_0bd0a1f3`

当前失败原因：
Calendar attendee search result was not locally confirmed by OCR。

能力：
1. 打开飞书日历。
2. 进入日历主 tab。
3. 点击创建日程。
4. 输入标题。
5. 用 OCR 日期网格选择目标日期。
6. 输入开始和结束时间。
7. 保存日程。
8. OCR 验证标题、日期、时间。

推荐命令：

```powershell
$env:DRY_RUN="false"
$env:CUA_LARK_PLACEHOLDER_SCREENSHOT="false"
$env:CUA_LARK_MODEL_PROVIDER="auto"
$env:CUA_LARK_ALLOW_CALENDAR_CREATE="true"
python -B cli.py run-case cases\calendar_create_event.yaml --auto-debug
```


2. 邀请参会人：能力已实现，最近回归失败

case：
`cases\calendar_invite_attendee_guarded.yaml`

最后已知真实通过报告：
`runs\reports\run_20260426_130145_6805e8ae`

最近失败报告：
`runs\reports\run_20260426_165410_4c3ec35b`

当前失败原因：
Calendar attendee search result was not locally confirmed by OCR。

能力：
1. 创建测试日程。
2. 在参会人输入框中搜索“李新元”。
3. OCR 定位并点击参会人搜索结果。
4. 条件式处理可能出现的添加参会人确认弹窗。
5. 保存日程。

推荐命令：

```powershell
$env:DRY_RUN="false"
$env:CUA_LARK_PLACEHOLDER_SCREENSHOT="false"
$env:CUA_LARK_MODEL_PROVIDER="auto"
$env:CUA_LARK_ALLOW_CALENDAR_CREATE="true"
$env:CUA_LARK_ALLOW_CALENDAR_INVITE="true"
python -B cli.py run-case cases\calendar_invite_attendee_guarded.yaml --auto-debug
```


3. 修改日程时间：能力已实现，最近回归失败

case：
`cases\calendar_modify_event_time_guarded.yaml`

最后已知真实通过报告：
`runs\reports\run_20260426_120155_ad2c1bea`

最近失败报告：
`runs\reports\run_20260426_165551_430bb4be`

当前失败原因：
最终 OCR 未找到日程标题，且修改后时间未命中。

能力：
1. 创建测试日程。
2. 输入初始开始/结束时间。
3. 将开始时间从 10:00 修改为 11:00。
4. 将结束时间从 10:30 修改为 11:30。
5. 保存并验证。

推荐命令：

```powershell
$env:DRY_RUN="false"
$env:CUA_LARK_PLACEHOLDER_SCREENSHOT="false"
$env:CUA_LARK_MODEL_PROVIDER="auto"
$env:CUA_LARK_ALLOW_CALENDAR_CREATE="true"
$env:CUA_LARK_ALLOW_CALENDAR_MODIFY="true"
python -B cli.py run-case cases\calendar_modify_event_time_guarded.yaml --auto-debug
```


4. 查看忙闲状态：能力已实现，最近回归失败

case：
`cases\calendar_view_busy_free_guarded.yaml`

最后已知真实通过报告：
`runs\reports\run_20260426_152523_2aec6992`

最近失败报告：
`runs\reports\run_20260426_165749_76613279`

当前失败原因：
2026-04-27 10:00 附近未看到李新元忙闲条目。

能力：
1. 打开日历主界面。
2. 搜索联系人“李新元”。
3. 点击联系人搜索结果或在已订阅状态下跳过重复点击。
4. 在时间轴上观察忙闲信息，不保存任何日程。
5. 验证右侧主时间轴区域存在忙闲相关证据。

关键修正：
1. 搜索结果右侧图标使用像素级状态判断。
2. 灰色图标表示可订阅/可选择，才点击。
3. 蓝色已选中图标表示已经订阅，跳过点击，避免再次点击触发“退订成功”。
4. 忙闲验证只看右侧主时间轴区域，避免左侧搜索结果、右侧聊天窗口和其他 UI 造成误判。
5. 验证要求联系人/自身/时间轴/日历网格/绿色忙闲标记等证据组合成立。

推荐命令：

```powershell
$env:DRY_RUN="false"
$env:CUA_LARK_PLACEHOLDER_SCREENSHOT="false"
$env:CUA_LARK_MODEL_PROVIDER="auto"
python -B cli.py run-case cases\calendar_view_busy_free_guarded.yaml --auto-debug
```


六、IM 当前进展摘要

IM 已从原始文本发送 MVP 扩展为完整建议测试场景，且 2026-04-26 18:45 前最新真实回归全部通过：
1. `cases\im_send_message_guarded.yaml`：发送文本消息，最新通过 `runs\reports\run_20260426_173819_7c6c0263`。
2. `cases\im_send_image_guarded.yaml`：发送图片消息，最新通过 `runs\reports\run_20260426_174305_4aaa8145`。
3. `cases\im_create_group_guarded.yaml`：创建群组，最新通过 `runs\reports\run_20260426_183354_ac0c06f0`。
4. `cases\im_mention_user_guarded.yaml`：@ 提及测试群成员，最新通过 `runs\reports\run_20260426_180101_f8712542`。
5. `cases\im_search_messages.yaml`：搜索消息记录，最新通过 `runs\reports\run_20260426_174845_c9d4c0b4`。
6. `cases\im_emoji_reaction_guarded.yaml`：表情回复，最新通过 `runs\reports\run_20260426_184529_54dfb4ac`。
7. `cases\im_full_suite.yaml`：IM 扩展 suite。

IM 基线发消息的关键约束仍保留：
1. 搜索结果必须 OCR 确认目标行。
2. 选中目标会话后直接输入，不额外点击、拖动或长按。
3. 真实发送必须开启 `CUA_LARK_ALLOW_SEND_MESSAGE=true`。
4. 可用 `CUA_LARK_ALLOWED_IM_TARGET` 限定测试群或测试联系人。
5. Guarded IM 动作前会确认当前右侧会话仍是允许目标。
6. IM 错误操作库会识别“打开错误会话”“停在全局搜索”“进入私聊”等错误状态并阻止继续乱点。
7. 建群流程已处理飞书白屏加载态，并用 OCR 定位成员输入框、群名输入框和创建按钮。
8. 表情回复先 OCR 定位目标消息，再只在消息附近寻找快捷表情按钮。


七、关键技术实现

1. 自然语言语义路由

主要文件：
1. `intent\parser.py`
2. `agent\nodes\plan_task.py`
3. `products\workflows.py`

已支持的 Docs / Calendar 意图：
1. `docs_create_doc`
2. `docs_rich_edit`
3. `docs_share_doc`
4. `calendar_create_event`
5. `calendar_invite_attendee`
6. `calendar_modify_event_time`
7. `calendar_view_busy_free`


2. OCR / 视觉 / 像素融合定位

主要文件：
1. `tools\vision\lark_locator.py`
2. `tools\vision\ocr_client.py`
3. `agent\nodes\decide.py`

已完成：
1. IM 搜索结果 OCR 精确目标行。
2. Docs 新建菜单、空白文档卡片、分享按钮、分享收件人输入框、收件人结果、确认按钮定位。
3. Calendar 创建按钮、保存按钮、日历页签、参会人搜索结果定位。
4. Calendar 日期选择器按年月标题和 7 列网格定位目标日期。
5. Calendar 忙闲联系人搜索结果和订阅状态图标定位。
6. 对 VLM 坐标做缩放校正，兼容“原始截图尺寸 -> resize 给模型 -> 模型输出坐标 -> 反投影回原始截图”的流程。


3. 条件式闭环动作

主要文件：
1. `core\schemas.py`
2. `tools\desktop\executor.py`
3. `agent\nodes\execute.py`

已完成动作类型：
1. `hover`
2. `focus_window`
3. `conditional_hotkey`
4. `conditional_click`

用途：
1. 只有检测到浮层时才按 Esc。
2. 只有检测到关闭按钮时才点击。
3. Docs 创建文档时使用 hover 打开菜单。
4. Calendar 清理旧编辑器、旧确认框、旧参会人弹窗和会议室侧栏。
5. 防止硬编码式开环操作。


4. 本地验证

主要文件：
1. `verification\registry.py`
2. `tools\vision\ocr_client.py`

已完成：
1. Docs 文档标题/正文可见性验证。
2. Docs 富文本标题和列表可见性验证。
3. Docs 分享弹窗和分享完成状态验证。
4. Calendar 日程标题、日期、时间验证。
5. Calendar 参会人输入/搜索结果验证。
6. Calendar 忙闲时间轴验证。


八、当前已有 case

IM：
1. `cases\smoke_search_only.yaml`
2. `cases\im_search_only.yaml`
3. `cases\im_send_message_guarded.yaml`
4. `cases\im_send_image_guarded.yaml`
5. `cases\im_create_group_guarded.yaml`
6. `cases\im_mention_user_guarded.yaml`
7. `cases\im_search_messages.yaml`
8. `cases\im_emoji_reaction_guarded.yaml`
9. `cases\im_full_suite.yaml`

Docs：
1. `cases\docs_open_smoke.yaml`
2. `cases\docs_create_doc.yaml`
3. `cases\docs_rich_edit_guarded.yaml`
4. `cases\docs_share_doc_guarded.yaml`

Calendar：
1. `cases\calendar_create_event.yaml`
2. `cases\calendar_invite_attendee_guarded.yaml`
3. `cases\calendar_modify_event_time_guarded.yaml`
4. `cases\calendar_view_busy_free_guarded.yaml`

Suite：
1. `cases\safe_smoke_suite.yaml`
2. `cases\docs_calendar_extended_suite.yaml`


九、推荐验收顺序

1. 导入检查：

```powershell
$env:PYTHONIOENCODING="utf-8"
$env:PYTHONDONTWRITEBYTECODE="1"
python -B -c "import cli; import app.main; import tools.vision.ocr_client; print('imports ok')"
```

2. 截图诊断：

```powershell
python cli.py screenshot-diagnostics --configured-only
python cli.py inspect-screen
```

3. mock / dry-run：

```powershell
$env:CUA_LARK_MODEL_PROVIDER="mock"
$env:DRY_RUN="true"
$env:CUA_LARK_PLACEHOLDER_SCREENSHOT="true"
python -B cli.py run-suite cases\safe_smoke_suite.yaml --auto-debug
python -B cli.py run-suite cases\docs_calendar_extended_suite.yaml --auto-debug
```

4. Docs 真实回归：

```powershell
$env:CUA_LARK_MODEL_PROVIDER="auto"
$env:DRY_RUN="false"
$env:CUA_LARK_PLACEHOLDER_SCREENSHOT="false"
$env:CUA_LARK_ALLOW_DOC_CREATE="true"
python -B cli.py run-case cases\docs_create_doc.yaml --auto-debug
python -B cli.py run-case cases\docs_rich_edit_guarded.yaml --auto-debug
```

5. Docs 分享真实回归：

```powershell
$env:CUA_LARK_ALLOW_DOC_SHARE="true"
$env:CUA_LARK_ALLOWED_DOC_SHARE_RECIPIENT="李新元"
python -B cli.py run-case cases\docs_share_doc_guarded.yaml --auto-debug
```

6. Calendar 真实回归：

```powershell
$env:CUA_LARK_MODEL_PROVIDER="auto"
$env:DRY_RUN="false"
$env:CUA_LARK_PLACEHOLDER_SCREENSHOT="false"
$env:CUA_LARK_ALLOW_CALENDAR_CREATE="true"
python -B cli.py run-case cases\calendar_create_event.yaml --auto-debug
```

7. Calendar 邀请和修改真实回归：

```powershell
$env:CUA_LARK_ALLOW_CALENDAR_INVITE="true"
python -B cli.py run-case cases\calendar_invite_attendee_guarded.yaml --auto-debug

$env:CUA_LARK_ALLOW_CALENDAR_MODIFY="true"
python -B cli.py run-case cases\calendar_modify_event_time_guarded.yaml --auto-debug
```

8. Calendar 忙闲状态真实回归：

```powershell
python -B cli.py run-case cases\calendar_view_busy_free_guarded.yaml --auto-debug
```


十、当前总体完成度估计

1. 工程结构：90%
2. CLI/API：80%
3. 截图诊断：90%
4. IM 扩展 E2E：95%
5. Docs 创建/编辑/分享 E2E：70%-80%，创建最新通过，富文本/分享需修复入口回归。
6. Calendar 创建/邀请/修改/忙闲 E2E：65%-75%，均有已知通过报告，但最近一轮回归失败。
7. 报告体系：85%
8. 安全调试闭环：85%
9. OCR/视觉/像素混合定位：80%
10. 自愈重规划：40%
11. 交付文档/Demo：65%

整体 MVP 完成度：
约 78%-85%。IM 已进入可回归状态；Docs / Calendar 下一阶段重点是恢复最近回归稳定性。


十一、下一步建议

优先级 1：回归稳定性
1. 优先修复 Docs 富文本/分享最近卡住的 Docs screen 本地确认问题。
2. 优先修复 Calendar 参会人搜索 OCR、最终日程 OCR、忙闲时间轴证据识别问题。
3. 对 Docs 三条真实链路各跑 2-3 次。
4. 对 Calendar 四条真实链路各跑 2-3 次。
5. IM 六条核心链路可作为当前稳定基线继续回归。

优先级 2：报告字段补齐
1. `RuntimeContext` 增加 `allow_doc_share`、`allowed_doc_share_recipient`、`allow_calendar_modify`。
2. `report_writer` 打印这些字段。
3. summary 中补充自然语言解析出的 share_recipient、new_event_time 等关键字段。

优先级 3：Accessibility / OCR 融合
1. 引入 Windows UI Automation 获取控件 bbox。
2. 用 OCR 确认文字，用 accessibility 确认控件角色和 bbox。
3. 最终点击选择目标 bbox 中心或安全点击区域。

优先级 4：评估材料
1. 更新 `ACCEPTANCE.md`。
2. 更新 `docs\evaluation.md` 的真实报告表。
3. 准备 Demo 视频和答辩脚本。
4. 明确哪些截图报告不适合提交到 GitHub。


十二、注意事项

1. 永远不要提交 `.env`。
2. 永远不要提交 API Key、Authorization header、真实 cookies。
3. `runs/` 报告截图可能包含真实桌面内容，默认不要提交到 GitHub。
4. 真实 IM 发送只对测试联系人或测试群执行。
5. Docs / Calendar 创建只使用无害测试标题和测试内容。
6. Docs 分享只分享给明确测试联系人，例如“李新元”。
7. 如果截图黑屏、窗口不对或 mock 回退，不要强行真实执行。


十三、结论

当前项目已经具备可演示的飞书桌面 GUI Agent MVP，并且已经从“单条创建链路”推进到“IM / Docs / Calendar 多场景覆盖”：
1. IM 已覆盖文本、图片、建群、@ 提及、搜索消息、表情回复，并且最新真实回归全部通过。
2. Docs 已覆盖创建、编辑、标题/列表、分享；创建最新通过，富文本/分享最近回归失败但有已知通过报告。
3. Calendar 已覆盖创建、邀请参会人、修改时间、忙闲状态查看；四项均有已知通过报告，但最近回归失败，需要优先恢复稳定性。
4. 每条链路都有 guarded 安全开关和可回放报告。
5. 定位策略已经从固定坐标转向 OCR、视觉、像素状态和条件式动作融合。

下一阶段建议从“能力实现”转向“多轮回归稳定、报告字段完善和交付材料整理”。
